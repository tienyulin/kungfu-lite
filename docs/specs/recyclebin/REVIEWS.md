# 盲審記錄

## query-recyclebin-api — 盲審第 1 輪（2026-08-08）

| # | 嚴重度 | 發現（引原文） | 判定 | 處置 |
|---|--------|---------------|------|------|
| 1 | MED | 時間截斷方向未明定：「來源時間有微秒就截斷」不明確是捨去還是四捨五入 | 成立 | 共通規範補「截斷＝捨去小數，不四捨五入」 |
| 2 | MED | 「空白」定義不明確：欄位值含前後空白時的判定標準 | 成立 | 共通規範補「欄位值去除前後空白（str.strip()，含 tab 與換行）後為空即視為空白，回 422」 |
| 3 | MED | 錯誤格式中 message 與 error.status 的關係未明確 | 成立 | 錯誤格式段補「message 的文字內容不屬於 API 契約，程式判斷一律依 error.status」 |
| 4 | MED | reason 值域與 CAN_UNDROP='NO' 的對應關係未明文 | 成立 | 名詞定義補「CAN_UNDROP='NO' 一律對應 reason=SPACE_RECLAIMED」 |
| 5 | LOW | 單表查詢時「取最新」的排序規則應同列全部 | 成立 | 單表查詢的「取最新」補「排序規則同列全部：DROPTIME 由新到舊，同秒時以 OBJECT_NAME 字典序由大到小」 |
| 6 | MED | 概述未明確本 API 只讀取、不做變更 | 成立 | 概述補「本 API 只讀取資料，不做任何變更」 |
| 7 | MED | URL 路徑參數解碼時機未定義 | 駁回 | framework 標準行為，不影響對外契約 |
| 8 | LOW | message 具體文案應如何編寫 | 駁回 | 第 3 項補句後，message 內容不屬 API 契約，故無需規定文案 |

結果：HIGH 0 / MED 5 / LOW 1 → 通過（所有成立項目已修正）

---

## restore-dropped-table-api — 盲審第 1 輪（2026-08-08）

| # | 嚴重度 | 發現（引原文） | 判定 | 處置 |
|---|--------|---------------|------|------|
| 1 | MED | 時間截斷方向未明定：「來源時間有微秒就截斷」不明確是捨去還是四捨五入 | 成立 | 共通規範補「截斷＝捨去小數，不四捨五入」 |
| 2 | MED | 「空白」定義不明確：欄位值含前後空白時的判定標準 | 成立 | 共通規範補「欄位值去除前後空白（str.strip()，含 tab 與換行）後為空即視為空白，回 422」 |
| 3 | MED | 錯誤格式中 message 與 error.status 的關係未明確 | 成立 | 錯誤格式段補「message 的文字內容不屬於 API 契約，程式判斷一律依 error.status」 |
| 4 | MED | restore_time 的定義與 mock 行為未明文 | 成立 | restore_time 明定為「FLASHBACK 執行成功並完成驗證時的系統時間」；mock 的 restore_time 回當下系統時間；測試只斷言格式不斷言值 |
| 5 | MED | FLASHBACK 指令中表名與新表名的大小寫處理未明文 | 成立 | 處理流程的 FLASHBACK 指令補「表名與新表名用轉大寫後的值」 |
| 6 | LOW | URL 路徑參數解碼時機未定義 | 駁回 | framework 標準行為，不影響對外契約 |
| 7 | LOW | message 具體文案應如何編寫 | 駁回 | 第 3 項補句後，message 內容不屬 API 契約，故無需規定文案 |
| 8 | LOW | 概述應解釋 index/trigger 用途、回收筒保留時間、CAN_UNDROP 變 NO 的機制、無法救時的替代方案 | 駁回 | SOP 未定義，spec 不編造業務邏輯；這些屬 Oracle 文件或 SOP 內容 |
| 9 | LOW | 第 1–3 節讀者能否說出這個 API 做什麼、誰能用、最需要小心的操作 | 駁回（改判） | 本 API 是查詢操作，無修改風險，不存在「最需要小心的操作」；第 6 項補句（「本 API 只讀取資料，不做任何變更」）已明確此點 |
| 10 | LOW | JSON 欄位順序、README 排版、mock 內部驗證實作方式 | 駁回 | 不屬 API 契約；實作自由度 |

結果：HIGH 0 / MED 5 / LOW 5(駁回) → 通過（所有成立項目已修正）
